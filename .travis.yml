dist: trusty

language: java

jdk:
  - oraclejdk13

services:
  - postgresql
  
addons:
  postgresql: "9.5"

install: true

env:
 global:
   - CODECOV_TOKEN="f6b39483-2825-46e8-ac4f-e906544d3611"

before_install:
  - psql -c 'create database "buy_manager";' -U postgres

script:
  - chmod +x ./.ci/script.sh 
  - ./.ci/script.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
